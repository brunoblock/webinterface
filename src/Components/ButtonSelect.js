import styled from 'styled-components'

import Button from '@component/Button'

const ButtonSelect = styled(Button.withComponent('select'))`
  padding: 0.5565em .8em 0.43em
`

export default ButtonSelect